#include <stdio.h>

int main() {
    int input;
    scanf("%d", &input);

    int num = 1;
    int sum = 0;

    for(int i = 1; i <= input, i++;){
        for(int j = 1; j <= i; j++) {
            sum += num;
            printf("%d ", num);
        }
    printf("\n");
    } 
}